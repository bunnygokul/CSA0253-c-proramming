#include<stdio.h>
main()
{
    str[50];
inti,length = 0;

printf("\nEnter the String: ");
get(str);
for(i=0; str[i]!='\0'; i++)
{
length++;
}
printf("\nThe length of the string is %c.", count);
getch();
}
